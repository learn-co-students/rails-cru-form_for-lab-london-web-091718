<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Edit Song</h1>
<p><%= link_to "Cancel", songs_url %></p>
<hr>
    <%= form_for @song do |f| %>

    <p><%= f.label :name %>
    <%= f.text_field :name %></p>

    <p><%= f.label "Select an Artist:" %>
    <%= f.collection_select :artist_id, @artist, :id, :name %></p>

    <p><%= f.label "Select a Genre:" %>
    <%= f.collection_select :genre_id, @genre, :id, :name %></p>
<hr>
    <%= f.submit %>

    <% end %>
  </body>
</html>
