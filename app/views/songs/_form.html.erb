<% if flash[:errors] %>
  <% flash[:errors].each do |e| %>
    <%= e %>
  <% end %>
<% end %>

<%= form_for(@song) do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :artist_id%>
  <%= f.text_field :genre_id%>
  <%= f.submit id:"submit" %>
<% end %>
