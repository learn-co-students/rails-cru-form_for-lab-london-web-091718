<% if flash[:errors] %>
  <% flash[:errors].each do |e| %>
    <%= e %>
  <% end %>
<% end %>

<%= form_for(@artist) do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :bio %>
  <%= f.submit id: "submit" %>
<% end %>
