<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Create a New Artist</title>
  </head>
  <body>
    <h1>New Artist Form</h1>
    <%= form_for @artist do |f| %>

    <p><%= f.label :name %>
        <%= f.text_field :name %></p>

    <p><%= f.label :bio %>
        <%= f.text_area :bio %></p>

    <p><%= f.submit %></p>
    <% end %>

  </body>
</html>
